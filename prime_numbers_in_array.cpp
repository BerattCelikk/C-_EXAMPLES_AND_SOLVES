/*Code the program that finds and displays the prime numbers of an array*/

/*#include <iostream>
#include <vector>

int main() {
    int n;
    std::cout << "Enter length of the array: ";
    std::cin >> n;

    std::vector<int> array(n);


    for (int i = 0; i < n; i++) {
        std::cout << "Enter a number: ";
        std::cin >> array[i];
    }

    std::cout << "Prime Numbers: ";


    for (int i = 0; i < n; i++) {
        int number1 = array[i];
        bool isprime = true;


        if (sayi <= 1) {
            isprime = false;
        } else {

            for (int j = 2; j * j <= sayi; j++) {
                if (number1 % j == 0) {
                    isprime = false;
                    break;
                }
            }
        }

        if (isprime) {
            std::cout << number1 << " ";
        }
    }

    std::cout << std::endl;

    return 0;
}*/
